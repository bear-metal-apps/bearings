# honeycomb

Honeycomb is the Bearings backend service built with Dart Frog.

## Local development

From the repo root:

1. Run `dart pub get`
2. Run `melos run analyze` and `melos run test` as needed
3. Start the server from `backend/honeycomb` with your usual Dart Frog workflow
